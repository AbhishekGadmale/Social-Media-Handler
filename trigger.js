const { PrismaClient } = require('@agency-os/database'); const { Queue } = require('bullmq'); const prisma = new PrismaClient(); async function run() { const acc = await prisma.socialAccount.findFirst({where: {status: 'ACTIVE'}}); if (!acc) return console.log('No active account'); const q = new Queue('sync', {connection: {host: 'localhost', port: 6379}}); await q.add('sync-account', { socialAccountId: acc.id, workspaceId: acc.workspaceId }); console.log('Job enqueued for account', acc.id); await prisma.$disconnect(); process.exit(0); } run();
