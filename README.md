# Agency OS

## Setup

1. Start databases:
   ```sh
   docker compose up -d
   ```
2. Install dependencies:
   ```sh
   pnpm install
   ```
3. Copy environment variables:
   ```sh
   cp .env.example .env
   ```
4. Start development server:
   ```sh
   pnpm dev
   ```
