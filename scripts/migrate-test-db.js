const { execSync } = require('child_process'); require('dotenv').config(); const testUrl = process.env.TEST_DATABASE_URL; if (!testUrl) throw new Error('TEST_DATABASE_URL missing'); console.log('Migrating test DB...'); execSync('npx prisma migrate deploy', { env: { ...process.env, DATABASE_URL: testUrl }, stdio: 'inherit', cwd: 'packages/database' }); console.log('Test DB migrated successfully!');
